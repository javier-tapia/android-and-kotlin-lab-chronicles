package com.example.android_and_kotlin_lab_chronicles.experiments.kotlin_pure.async_and_concurrency

/**
 * Experimentos: StateFlow y SharedFlow (Hot Streams).
 *
 * Temas:
 * - StateFlow: Gestión de estado atómico y replay del último valor.
 * - SharedFlow: Emisión de eventos y configuración de replay/extraBufferCapacity.
 * - Conversión: stateIn() y shareIn() para convertir flujos fríos en calientes.
 */
object StateAndSharedFlowsLab {
    // Espacio para lógica de estados y eventos reactivos compartidos
}
