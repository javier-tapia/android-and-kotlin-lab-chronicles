package com.example.android_and_kotlin_lab_chronicles.experiments.integrations.firebase

/**
 * Experimentos: Firebase Authentication (Auth).
 *
 * Temas:
 * - Registro e Inicio de sesión con Email y Password.
 * - Gestión de estados de usuario (currentUser, signOut).
 * - Proveedores de terceros (conceptos base).
 */
object FirebaseAuthLab {
    // Espacio para lógica de autenticación
}
