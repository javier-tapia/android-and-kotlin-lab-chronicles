package com.example.android_and_kotlin_lab_chronicles.experiments.kotlin_pure.async_and_concurrency

/**
 * Experimentos: Kotlin Flows (Cold Streams).
 *
 * Temas:
 * - Creación: flow {}, asFlow(), flowOf().
 * - Operadores intermedios: map, filter, transform, take.
 * - Operadores de tamaño y tiempo: debounce, sample, conflate.
 * - Combinación: zip, combine, flatMapMerge, flatMapConcat.
 * - Recolección: collect, collectLatest, single, first.
 */
object FlowsLab {
    // Espacio para lógica de flujos y operadores reactivos
}
