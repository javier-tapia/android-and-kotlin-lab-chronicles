package com.example.android_and_kotlin_lab_chronicles.experiments.kotlin_pure.collections

/**
 * Experimentos: Adición y Eliminación de elementos en colecciones mutables.
 *
 * Temas: add, addAll, +=, remove, removeAll, retainAll, clear, -=
 *
 * Nota: En Kotlin 2.x, estas operaciones siguen siendo el estándar para [MutableList] y [MutableSet].
 */
object AddingAndRemoving {
    // Vacío para experimentación
}

/**
 * Experimentos: Operadores plus (+) y minus (-) para crear nuevas colecciones inmutables.
 */
object Operators {
    // Vacío para experimentación
}
